<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="fdm_address_inherit" inherit_id="website_sale.address">
        <xpath expr="//t[contains(@t-if, 'partner_id == website_sale_order.partner_shipping_id.id == website_sale_order.partner_invoice_id.id')]"
               position="replace">
            <div class="alert alert-warning" role="alert" t-if="not only_services">
                <h4 class="alert-heading">Be aware!</h4>
                <p groups="sale.group_delivery_invoice_address">
                    You are editing your <b>billing and shipping</b> addresses at the same time!
                    <br/>
                    If you want to modify your shipping address, create a <a href='/shop/address'>new address</a>.
                </p>
            </div>
        </xpath>
    </template>
</odoo>
